# Mini App MVP: приемка в `dev`

Дата: 31.05.2026  
Статус: `done`

## 1. Сводка

1. Этап 5 (регрессии и стабилизация) закрыт.
2. Автопроверки на текущем состоянии `dev` проходят: `ruff` + `pytest` (`56 passed`).
3. Бот остается рабочим параллельным каналом, Mini App развивается как дополнение.
4. Для локальной проверки без конфликта `getUpdates` используется режим `TELEGRAM_POLLING_ENABLED=false` для Mini App smoke.

## 2. Checklist приемки MVP

- [x] 1. Mini App открывается из Telegram и через direct link.
- [x] 2. Успешная авторизация пользователя.
- [x] 3. Рабочий сценарий `Записаться` до создания заявки.
- [x] 4. Рабочий раздел `Мои заявки` (список, изменение цели, отмена).
- [x] 5. Рабочие разделы `Профиль`, `Поддержка`, `Уведомления`.
- [x] 6. Полный админ-сценарий обработки заявки.
- [x] 7. Настройки расписания доступны администратору.
- [x] 8. Google OAuth сценарий доступен админу.
- [x] 9. Проверено удаление/анонимизация данных пользователя.
- [x] 10. Проверено, что существующий Telegram-бот продолжает работать без регрессий.
- [x] 11. `ruff` и `pytest` проходят.
- [x] 12. Изменения остаются в `dev` до отдельного решения о merge в `main`.

## 3. Что подтверждено автоматически

1. `.venv\Scripts\python -m ruff check app tests` — успешно.
2. `.venv\Scripts\python -m pytest -q` — `56 passed`.
3. Набор Mini App security тестов (`tests/test_miniapp_security.py`) включен в общий прогон.
4. Точечный прогон приемочных тестов: `tests/test_miniapp_routes.py` + `tests/test_stage6_google_calendar.py` — `7 passed`.
5. API-smoke локального контура:
   - `GET /miniapp` -> `200`;
   - `POST /api/miniapp/auth/telegram` -> `200` (валидный `initData`);
   - `GET /api/miniapp/admin/google/oauth/url` -> `200`;
   - `POST /api/miniapp/admin/google/oauth/exchange` с тестовым невалидным кодом -> контролируемый ответ `400` (`Google OAuth code exchange failed.`), endpoint доступен.

## 4. Что подтверждено вручную

1. Ключевые пользовательские функции Mini App по UI подтверждены владельцем проекта.
2. Переход к следующему этапу после проверки функций владельцем согласован.

## 5. Итог приемки

1. Этап 6 закрыт: все пункты checklist Mini App MVP выполнены.
2. Mini App MVP принят в ветке `dev`.
3. Telegram-бот остается рабочим параллельным каналом на всех этапах.
